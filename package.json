{
  "name": "alsham-business-os",
  "version": "0.0.0",
  "private": true,
  "description": "ALSHAM Business OS — Sistema Operacional Empresarial Modular. Etapa 0: fundação (esqueleto e documentação, sem código de produto).",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "tsc --noEmit -p packages/config/tsconfig.json && tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/workflow/tsconfig.json && tsc --noEmit -p packages/billing/tsconfig.json && tsc --noEmit -p packages/finance-reconciliation/tsconfig.json && tsc --noEmit -p packages/marketing/tsconfig.json && tsc --noEmit -p apps/portal/tsconfig.json",
    "build:portal": "pnpm --filter @alsham/portal build",
    "test": "node --test \"packages/*/src/**/*.test.ts\"",
    "test:docs": "node --test .github/scripts/verificar-estado-docs.test.mjs",
    "verificar:docs": "node .github/scripts/verificar-estado-docs.mjs"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
