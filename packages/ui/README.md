# packages/ui

**Propósito:** o design system da plataforma — componentes, tokens e temas usados por `apps/admin`, `apps/portal` e `apps/store`. White label de nascença: a identidade visual é do tenant, nunca fixa em código.

**Fase do roadmap:** Fase 1 — Core (suporte às telas do Core).

**De onde será minerado:** origem candidata é o `packages/design-system` do `alsham-events-os`. **NÃO VERIFICADO** — o Balanço de Tecnologia não registra estado para design system; conferir antes de adotar.

**Regra herdada da Casa:** zero cor hardcoded. Tudo por variável CSS, com light e dark funcionais.

**Status:** NÃO INICIADO.
