{
  "name": "@alsham/erisk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": {
    "@alsham/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "description": "Módulo 75 — Risco Corporativo. ABRE o Domain GRC. O risco estratégico do negócio (sem project_id — o DIVERGE do risk, project-scoped); mantém a física do ciclo (mitigated reabre, closed terminal); treatment accept/mitigate/transfer/avoid."
}
