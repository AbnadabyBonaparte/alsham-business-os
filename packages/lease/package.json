{
  "name": "@alsham/lease",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": { "@alsham/core": "workspace:*" },
  "devDependencies": { "@types/node": "^22.10.2", "typescript": "^5.7.2" },
  "description": "Módulo 39 — Locação de Lojistas (Vertical Shopping Centers). Domínio puro: camada fina sobre o ctr (vigência/reajuste/renovação/rescisão) e o mall (cadastro do lojista) — id solto para os dois. Só o termo comercial sobre vendas (texto livre) e o relatório mensal de vendas, imutável."
}
