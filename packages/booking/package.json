{
  "name": "@alsham/booking",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": { "@alsham/core": "workspace:*" },
  "devDependencies": { "@types/node": "^22.10.2", "typescript": "^5.7.2" },
  "description": "Módulo 97 — Agendamento (Vertical Beleza). Domínio puro: a agenda de serviços do salão. Reaproveita a física do no-show do appointment (scheduled → attended | no_show | cancelled, três terminais); cliente do crm por id solto (não paciente, não PHI), serviço texto livre, profissional id solto."
}
