/**
 * @alsham/logperf — Módulo 52: Performance Logística.
 *
 * Domínio puro. A migration `0067_logperf.sql` é o chão; este pacote é a regra
 * que as telas consomem (Regra de Ouro).
 */

export * from './types.ts';
export * from './logperf.ts';
export { MANIFEST, PERMISSIONS, EVENTS } from './manifest.ts';
