# packages/config

**Propósito:** configuração compartilhada do monorepo — presets de TypeScript, lint, formatação e build que apps e packages estendem, para que a plataforma inteira obedeça a uma régua só.

**Fase do roadmap:** Fase 1 — Core (encanamento; precede os módulos).

**De onde será minerado:** **NÃO VERIFICADO** — nenhum balanço registra peça de config compartilhada. Obra nova, e deliberadamente pequena.

**Não confundir com cofre de segredos.** Segredo de tenant é assunto do Core/billing (padrão `private.config` da casa-bonaparte). Aqui nunca entra valor real.

**Status:** NÃO INICIADO.
