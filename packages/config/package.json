{
  "name": "@alsham/config",
  "version": "0.0.0",
  "private": true,
  "description": "Constantes canônicas do ALSHAM Business OS. Só o que é verificável e institucional — nenhum número de marketing, nenhum dado de cliente, nenhum segredo.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
