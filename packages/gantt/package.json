{
  "name": "@alsham/gantt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": {
    "@alsham/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "description": "Módulo 59 — Gantt / Dependências entre marcos. Domínio puro: a aresta de precedência entre marcos (id solto ao sched); registro MUTÁVEL (a aresta some quando o plano muda); sem caminho crítico nem cálculo de datas."
}
