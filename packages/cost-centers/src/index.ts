/**
 * @alsham/cost-centers — Módulo 28: Centros de Custo & Rateio.
 *
 * Domínio puro. A migration `0043_cc.sql` é o chão; este pacote é a regra
 * que as telas consomem (Regra de Ouro).
 */

export * from './types.ts';
export * from './cost-centers.ts';
export { MANIFEST, PERMISSIONS, EVENTS } from './manifest.ts';
