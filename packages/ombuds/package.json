{
  "name": "@alsham/ombuds",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": {
    "@alsham/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "description": "Módulo 91 — Ouvidoria (Lei 13.460). Vertical Governo. A manifestação nasce imutável; o tratamento anda (received→under_review→answered/dismissed). ⭐⭐ Anonimato físico reaproveitado do whistle: se anônima, o cidadão NUNCA é gravado (nem na tela, nem no banco). Protocolo público carimbado pelo servidor. Confidencialidade na RLS."
}
